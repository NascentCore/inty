---
description:
globs: **/*.py
alwaysApply: false
---

# Python

## Logging

* Use the already present logging api
* If the file has no logging api, use loguru
* Avoid enclosing functional code with try...except block
  The idea is to expose potential bugs
