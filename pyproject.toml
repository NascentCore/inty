[project]
name = "inty-backend"
version = "1.1.0"
description = "Inty backend server"
readme = "README.md"
requires-python = ">=3.12"

# 用于运行 uv ty check 命令，排除不需要类型检查的目录。
[tool.ty.src]
exclude = [
    "experimental",
    "evaluation",
    "scripts",
    "tests",
    "**/migrations",
]
