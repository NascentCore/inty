# `structured_agentic_brain/`：结构化智能体脑原型

**一句话**：用 **最小可运行** 的 Python 原型探索「有结构的 agentic 循环」；优先证明 **主路径行为**，不为边角写复杂框架。

## 习惯

- **数据**：输入输出一律 **Pydantic**。
- **入口**：命令行从 **`main.py`** 进入，不使用包内隐藏 `__main__.py`。
- **语言**：注释可用中文；面向终端或对外样本输出默认 **英文**。
- **错误处理**：够用即可，避免企业级防御式分支淹没核心实验。
