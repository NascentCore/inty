# LangSmith trace fixtures

This directory was generated by the coding agent entirely.

Archived LangSmith traces for companion-harness debugging and issue reproduction.

| Trace ID | Issue | Notes |
|----------|-------|-------|
| `019ed438-f634-7941-b109-cbdc9e2e9510` | #3468 | AUTONOMY inner-tick: `LIFE_CURRENTS.md` rewrite + `techno_core_record_event` (agent `4b180c33`) |

Download fresh copies: `.cursor/skills/langsmith-download-run` (`download_run.py --trace-id …`). Omit `--project-name` when the trace lives outside your local `LANGSMITH_PROJECT`.
