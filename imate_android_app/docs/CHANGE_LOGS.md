# iMate Change Logs

> CREATED_BY_AGENT
> Counterpart to [android_app/docs/CHANGE_LOGS.md](../../android_app/docs/CHANGE_LOGS.md).

> If injected into the Inty official assistant system message, strip lines starting with `>`.

- Only **user-visible** changes for **release** builds are recorded here.
- Omit changes that affect **debug-only** behavior unless testers need them called out separately.
- Format: one short sentence suitable for **Google Play release notes**, then optional indented detail.

<!-- TODO(follow-up): Backfill notable shipped releases once there is a stable Play track history for `com.inty.imate`. -->

## 2026-04-20

- Google sign-in: when Play services hits a connectivity-style failure during account sign-in, the toast explains checking network/VPN and Play services instead of showing only a cryptic `[16]` message.

## Template (remove when first real entry lands)

<!--
## YYYY-MM-DD

- One-line summary for Play store notes.

    - Optional bullets for testers or detailed changelog.
-->
