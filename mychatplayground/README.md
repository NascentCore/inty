## Mychatplayground

一个用于 **边改提示词边聊天验证** 的本地 Playground（OpenRouter 前端直连）。

### 启动

```bash
cd web
npm install
npm run dev
```

打开终端提示的本地地址后：
- 左侧填写 OpenRouter API Key、选择模型、编辑初始提示词/示例/变量、保存版本、导入导出
- 右侧进行上下文聊天；支持 **Cmd+Enter** 发送；支持“一键清空聊天”


