# 图片超分

目标：检查已上线的 Firebase 埋点数据，确认用户是否真实使用该功能，再决定是否继续投入开发。

## 待办

1. 拉取事件 `image_viewer_upscale` 的近 7 天与近 14 天数据（按 `action` 维度拆分）：
   - `click_button`
   - `open_options_dialog`
   - `open_unlock_dialog`
   - `unlock_attempt`
   - `unlock_success_credits`
   - `unlock_failed_not_enough_credits`
   - `apply_factor`
2. 统计核心转化漏斗：
   - `click_button` -> `open_options_dialog`
   - `open_unlock_dialog` -> `unlock_success_credits`
   - `click_button` -> `apply_factor`
3. 统计倍率偏好（`target_factor`：1x / 2x / 4x）与订阅分层（`is_subscribed`）下的行为差异。
4. 输出结论并在本文件补充“是否继续开发图片超分”的决策（继续 / 暂缓）及下一步建议。
