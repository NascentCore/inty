# Keep Talking 功能说明

## 功能概述

Keep Talking 功能是一个后台服务，用于在用户长时间没有回复时，让 AI Agent 主动延续对话。这个功能帮助保持对话的活跃度和连续性。

## 功能特性

- **自动监控**: 定期检查所有开启了 keep_talking 功能的聊天会话
- **智能判断**: 当任何聊天会话超过设定时间没有新消息时就会触发，无论最后一条消息是谁发送的
- **上下文延续**: 基于前面的聊天内容生成相关的延续对话
- **频次限制**: 防止过度发送，单次会话有最大 keep_talking 消息数量限制
- **用户控制**: 用户可以通过聊天设置开启/关闭此功能

## 配置说明

在 `config.yaml` 文件中添加以下配置：

```yaml
keep_talking:
  check_interval: 300 # 检查间隔（秒），默认5分钟
  max_idle_time: 1800 # 最大空闲时间（秒），默认30分钟
  max_keep_talking_messages: 3 # 单次会话最多发送的keep_talking消息数量
```

### 配置参数说明

- `check_interval`: 后台服务检查空闲会话的时间间隔（秒）
- `max_idle_time`: 用户没有回复的最大时间，超过此时间将触发 keep_talking 消息（秒）
- `max_keep_talking_messages`: 在用户重新开始对话之前，最多发送的 keep_talking 消息数量

## 数据库字段

在 `chat_settings` 表中：

- `keep_talking`: Boolean 字段，控制是否开启 keep_talking 功能，默认为 `true`

## API 接口

### 获取 Keep Talking 服务状态

```
GET /api/v1/chats/keep-talking/status
```

返回服务运行状态、配置参数和活跃会话统计。

### 手动触发检查

```
POST /api/v1/chats/keep-talking/check
```

手动触发一次 keep_talking 检查，用于测试和调试。

### 更新聊天设置

```
PUT /api/v1/chats/agents/{agent_id}/settings
```

可以通过此接口开启或关闭特定会话的 keep_talking 功能：

```json
{
  "keep_talking": true
}
```

## 工作流程

1. **启动服务**: 应用启动时自动启动 Keep Talking 后台服务
2. **定期检查**: 每隔 `check_interval` 秒检查一次所有活跃会话
3. **筛选会话**: 找出开启了 keep_talking 且超过 `max_idle_time` 没有新消息的会话
4. **验证条件**: 确认未达到 keep_talking 消息上限
5. **生成消息**: 基于聊天历史生成自然的延续对话
6. **发送响应**: 通过 Agent 发送 keep_talking 消息
7. **更新计数**: 记录已发送的 keep_talking 消息数量
8. **重置机制**: 用户发送新消息时自动重置计数

## 消息生成策略

Keep Talking 消息的生成基于以下策略：

- **上下文分析**: 分析最近的对话内容，理解当前话题
- **发送者适配**: 根据最后一条消息的发送者采用不同的延续策略
  - 用户最后发言：主动回应和提问，延续话题
  - Agent 最后发言：自然地继续话题，提供补充信息或不同角度
- **自然延续**: 生成与当前话题相关的问题或观点
- **多样化**: 使用多种提示词模板，避免重复
- **隐形触发**: 保持对话的自然性，避免显得突兀

## 示例场景

### 场景 1: 日常聊天

- 用户: "我今天工作很累"
- Agent: "工作确实很辛苦，你是做什么工作的呢？"
- _30 分钟后用户没有回复_
- Agent: "休息一下也很重要呢，你平时都有什么放松的方式吗？"

### 场景 2: 学习讨论

- 用户: "我在学习 Python 编程"
- Agent: "Python 是很好的入门语言！你现在学到哪个部分了？"
- _30 分钟后用户没有回复_
- Agent: "如果在学习过程中遇到困难，建议多动手实践，编程是需要大量练习的。你有在写一些小项目练手吗？"

### 场景 3: Agent 主动延续

- 用户: "最近工作压力好大"
- Agent: "工作压力确实会影响身心健康，建议你可以尝试一些放松的方式，比如运动、冥想或者听音乐。"
- _30 分钟后没有新消息_
- Agent: "除了放松技巧，合理安排工作时间也很重要。你平时是怎么管理时间的呢？或者有什么特别让你感到压力的工作任务吗？"

## 注意事项

1. **隐私保护**: Keep Talking 功能不会存储额外的用户数据，仅基于现有聊天历史
2. **资源管理**: 服务会自动清理长时间不活跃的会话计数
3. **用户控制**: 用户可以随时通过设置关闭此功能
4. **频次控制**: 避免过度打扰，有合理的频次限制

## 故障排除

### 服务未启动

检查应用启动日志，确认 Keep Talking 服务是否成功启动。

### 消息未发送

1. 检查聊天设置中 keep_talking 是否开启
2. 确认是否达到最大 keep_talking 消息数量限制
3. 验证空闲时间是否超过配置的阈值
4. 检查会话是否有消息历史

### 配置不生效

重启应用以使配置文件修改生效。

## 监控和调试

使用以下 API 接口进行监控和调试：

- 查看服务状态: `GET /api/v1/chats/keep-talking/status`
- 手动触发检查: `POST /api/v1/chats/keep-talking/check`
- 查看 Agent 状态: `GET /api/v1/chats/agents/status`

这些接口可以帮助了解服务运行状态和排查问题。
