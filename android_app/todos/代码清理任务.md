# 代码清理任务

基于当前编译/弃用警告整理的 TODO，关联任务已合并为同一项。

---

## 1. Activity 跳转迁移至 Routes

以下 Activity 的类级/启动方式已废弃，需统一改为通过 Routes 跳转：

| 原 Activity | 替代路由 |
|-------------|----------|
| `AvatarGenerateActivity` | `Routes.Create.AvatarGenerate` |
| `CreateRoleActivity` | `Routes.Create.CreateRole` |
| `AgentInfoActivity` | `Routes.Home.AgentInfoPage` |
| `ReportActivity` | `Routes.Me.reportPage()` |
| `RegInfoActivity` | `Routes.Home.RegInfo` |
| `ModifyProfileActivity` | `Routes.Me.ModifyProfile` |
| `SubsManageActivity` | `Routes.Me.SubsManagement` |

- [ ] 将所有直接 `startActivity(XXXActivity::class.java)` 的调用改为对应 Routes 入口
- [ ] 确认无遗漏后，可考虑移除 Activity 上的 `@Deprecated` 或保留作兼容说明

**涉及文件：** `AvatarGenerateActivity.kt`, `CreateRoleActivity.kt`, `AgentInfoActivity.kt`, `ReportActivity.kt`, `RegInfoActivity.kt`, `ModifyProfileActivity.kt`, `SubsManageActivity.kt` 及其调用方。

---

## 2. TopAppBar：centerAlignedTopAppBarColors → topAppBarColors

Material 3 中 `centerAlignedTopAppBarColors()` 已废弃，需改为 `topAppBarColors`。

- [ ] `AvatarGenerateActivity.kt`（约 156 行）
- [ ] `ReportScreen.kt`（约 142 行）
- [ ] `BoostLeaderboardScreen.kt`（约 166 行）

---

## 3. 聊天模块：agentInfo / _agentInfo 迁移为 agentFlow（本地 DB）

`ChatViewModel` 的 `_agentInfo` / `agentInfo` 已废弃，应改为使用 `agentFlow` 从本地数据库（Room）查询角色信息，并兼顾旧逻辑的数据同步。

- [ ] 在 ViewModel 中改为依赖 `agentFlow`，逐步移除或收敛对 `_agentInfo` 的写入与读取
- [ ] `ChatItem.kt`、`ChatPage.kt`、`ChatScreen.kt`、`ChatInput.kt` 等由 `viewModel.agentInfo` 改为使用 `agentFlow`（或由 ViewModel 暴露的基于 agentFlow 的 State）

**涉及文件：** `ChatViewModel.kt`, `ChatItem.kt`, `ChatPage.kt`, `ChatScreen.kt`, `ChatInput.kt`。

---

## 4. Explore：NestedScrollSource.Drag → UserInput

`ExploreScrollBehavior.kt` 中 `NestedScrollSource.Drag` 已废弃，由 `UserInput` 替代。

- [ ] 将 `Drag` 的引用替换为 `UserInput`，并验证滚动行为一致

**涉及文件：** `ExploreScrollBehavior.kt`（约 25 行）。

---

## 5. Messages：TabRow / Indicator / tabIndicatorOffset 迁移

Material 3 中 Tab 相关 API 已调整：

- `TabRow` → 使用 `PrimaryTabRow` 或 `SecondaryTabRow`
- `TabRowDefaults.Indicator` → 使用 `SecondaryIndicator`
- `Modifier.tabIndicatorOffset(currentTabPosition)` → 使用 `TabIndicatorScope.tabIndicatorOffset`

- [ ] 在 `MessagesPage.kt`（约 516–524 行）完成上述替换并核对样式与交互

**涉及文件：** `MessagesPage.kt`。

---

## 6. ModifyProfileActivity：getParcelableExtra 新 API

`getParcelableExtra(String)` 在 Java/Android 中已废弃，需使用带 `Class<T>` 的重载。

- [ ] 在 `ModifyProfileActivity.kt`（约 73 行）改为新 API，例如 `getParcelableExtra(key, T::class.java)`（或等价写法）

**涉及文件：** `ModifyProfileActivity.kt`。

---

## 7. VideoCacheManager：恒真条件

`VideoCacheManager.kt` 约 168 行存在“条件恒为 true”的告警，可能是冗余判断或逻辑错误。

- [ ] 检查该条件分支，删除冗余判断或修正业务逻辑

**涉及文件：** `VideoCacheManager.kt`。

---

## 8. ShareUtils：startActivity 废弃 API

`ActivityOptionsCompat.startActivity(...)` 或类似 `startActivity` 的静态调用在 Java 中已标记废弃。

- [ ] 在 `ShareUtils.kt`（约 22 行）改用当前推荐的启动 Activity 方式（如 `Context.startActivity` + `Bundle`/`ActivityOptions` 等）

**涉及文件：** `ShareUtils.kt`。
