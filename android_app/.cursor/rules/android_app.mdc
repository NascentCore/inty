---
description: Android app development rules for HeartMate project (2025.10)
globs: ["android_app/**/*.kt", "android_app/**/*.java", "android_app/**/*.xml", "android_app/**/*.kts"]
alwaysApply: true
---

# Android App Development Rules (2025.10)

## AI使用规范 (2025年最新标准)

### AI生成内容标识
- 在用户界面中明确标识AI生成的内容
- 遵循GB45438-2025国家强制标准要求

### AI安全与合规
- 禁止使用未经审核的AI模型
- 确保AI决策过程的透明度和可解释性
- 遵循欧盟AI法案和OpenAI最新使用指南
- 实施AI内容审核机制，防止恶意使用

### 数据隐私保护
- 使用AI处理用户数据时遵循GDPR和CCPA规范
- 实施数据最小化原则
- 确保用户数据的加密存储和传输
- 提供用户数据删除和修改的机制

## 项目架构

### 技术栈 (2025.10最新)
- **Kotlin 2.2.20 + Jetpack Compose 2025.10** 用于现代UI开发
- **MVVM/MVI架构** 使用BaseViewModel和BaseMVI模式
- **原生Intent导航系统** 替代第三方路由库
- **协程 1.10.2 + Flow** 用于异步编程和状态管理
- **Retrofit 3.0.0 + OkHttp 5.2.1** 用于网络请求
- **Coil 3.3.0** 用于图片加载
- **MMKV 2.2.4** 用于高效存储
- **Firebase 34.1.0** 用于后端服务
- **Media3 1.8.0** 用于音视频处理
- **CameraX 1.5.1** 用于相机功能

### 架构模式
- 使用MVI模式：`State + Intent + Event`
- ViewModel继承`BaseViewModel<IState, IIntent, IEvent>`
- Activity继承`BaseActivity`
- 使用`StateFlow`管理状态，`SharedFlow`管理事件
- 使用**Koin**进行依赖注入

## 代码规范

### 文件命名
- 使用下划线分隔：`chat_activity.kt`
- Activity文件以`Activity`结尾
- ViewModel文件以`ViewModel`结尾
- Screen文件以`Screen`结尾
- 组件文件使用PascalCase：`ChatTopBar.kt`

### 包结构
```
com.ai.inty/
├── activities/          # Activity类
├── viewmodels/         # ViewModel类
├── ui/
│   ├── screens/        # 页面级Compose组件
│   ├── components/     # 可复用Compose组件
│   └── theme/          # 主题相关
├── beans/              # 数据模型
├── net/                # 网络相关
├── utils/              # 工具类
└── billing/            # 计费相关
```

### Kotlin代码规范

#### 类定义
```kotlin
/** 类功能描述 */
class ChatActivity : BaseActivity() {
    
    companion object {
        /**
         * 启动Activity
         * @param context 上下文
         * @param param 参数说明
         */
        fun launch(context: Context, param: String) {
            context.startActivity(Intent(context, ChatActivity::class.java).apply {
                putExtra("param", param)
            })
        }
    }
    
    private val viewModel: ChatViewModel by viewModels()
}
```

#### ViewModel定义
```kotlin
/** ViewModel功能描述 */
class ChatViewModel : BaseViewModel<ChatState, ChatIntent, ChatEvent>() {
    
    override fun createInitialState(): ChatState = ChatState()
    
    override suspend fun processIntent(intent: ChatIntent) {
        when (intent) {
            is ChatIntent.SendMessage -> handleSendMessage(intent)
            is ChatIntent.LoadMessages -> handleLoadMessages()
        }
    }
    
    private suspend fun handleSendMessage(intent: ChatIntent.SendMessage) {
        // 业务逻辑
    }
}

// State定义
data class ChatState(
    val messages: List<Message> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null
) : BaseMVI.IState

// Intent定义
sealed class ChatIntent : BaseMVI.IIntent {
    data class SendMessage(val content: String) : ChatIntent()
    object LoadMessages : ChatIntent()
}

// Event定义
sealed class ChatEvent : BaseMVI.IEvent {
    data class ShowError(val message: String) : ChatEvent()
    object NavigateToLogin : ChatEvent()
}
```

#### Compose组件
```kotlin
/** 组件功能描述 */
@Composable
fun ChatScreen(
    modifier: Modifier = Modifier,
    viewModel: ChatViewModel = viewModel(),
    onNavigateToLogin: () -> Unit = {}
) {
    val state by viewModel.state.collectAsState()
    val event by viewModel.events.collectAsState()
    
    LaunchedEffect(event) {
        when (event) {
            is ChatEvent.NavigateToLogin -> onNavigateToLogin()
            is ChatEvent.ShowError -> ToastUtils.showToast(event.message)
            null -> {}
        }
        viewModel.clearEvent()
    }
    
    // UI实现
}
```

### 网络请求规范

#### API接口定义
```kotlin
interface IChatApi {
    @POST("chat/send")
    suspend fun sendMessage(@Body request: SendMessageRequest): HttpResult<SendMessageResponse>
}
```

#### Koin依赖注入配置
```kotlin
val networkModule = module {
    single<IChatApi> { get<Retrofit>().create(IChatApi::class.java) }
    single<IAgentApi> { get<Retrofit>().create(IAgentApi::class.java) }
    single<Retrofit> { /* Retrofit配置 */ }
}

val viewModelModule = module {
    viewModel { ChatViewModel(get()) }
    viewModel { AgentViewModel(get()) }
}

val appModule = listOf(networkModule, viewModelModule)
```

### 依赖注入规范 (Koin)
- 使用Koin进行依赖注入管理
- 在Application中初始化Koin：`startKoin { modules(appModule) }`
- 使用`by inject<T>()`获取依赖实例
- 使用`by viewModels()`获取ViewModel实例
- 使用`companion object`中的`launch`方法启动Activity
- 避免使用`@Autowired`、`@Route`、`@Singleton`注解
- 使用`single`定义单例，`factory`定义工厂模式
- 使用`module`组织相关依赖
- 在测试中使用`declareMock<T>()`进行模拟

### 状态管理规范
- 使用`StateFlow`管理UI状态
- 使用`SharedFlow`管理一次性事件
- 状态更新使用`updateState { currentState -> newState }`
- 事件发送使用`sendEvent(event)`

### 协程使用规范 (2025.10最新)
- UI相关操作使用`launchUI`
- 后台任务使用`launchBackground`
- 网络请求在`IO`线程执行
- 使用`SupervisorJob`避免子协程异常影响父协程
- 使用`CoroutineExceptionHandler`处理协程异常
- 遵循结构化并发原则，避免全局协程作用域
- 使用`async`进行并发操作，使用`awaitAll`等待结果

### 错误处理规范
- 网络错误使用`HttpResult`包装
- 业务错误使用`BusinessErrorCodes`定义
- 用户提示使用`ToastUtils.showToast`
- 日志记录使用Android标准Log或第三方日志库

### 测试规范
- 单元测试使用JUnit + MockK
- UI测试使用Espresso
- 集成测试使用Koin测试容器
- 测试文件以`Test`结尾

### 性能优化规范 (2025.10最新)
- 使用`remember`缓存计算结果
- 使用`LazyColumn`处理长列表
- 图片加载使用Coil的`AsyncImage`
- 避免在Compose中执行耗时操作
- 使用`derivedStateOf`优化状态计算
- 使用`LazyVerticalGrid`处理网格布局
- 实施图片懒加载和预加载策略
- 使用`Modifier.drawWithContent`优化绘制性能
- 遵循Compose重组最小化原则

### 安全规范 (2025.10最新)
- 敏感信息使用`MMKV`加密存储
- 网络请求使用HTTPS和证书锁定
- 用户认证使用Firebase Auth + Credential Manager
- 支付相关使用Google Play Billing
- 实施代码混淆和反调试保护
- 使用Android Keystore保护密钥
- 遵循OWASP移动安全指南
- 实施运行时应用自保护(RASP)
- 使用生物识别认证增强安全性

### 代码质量规范
- 使用Kotlin代码风格
- 避免魔法数字，定义常量
- 使用有意义的变量名
- 添加必要的注释
- 遵循单一职责原则

### 构建配置规范
- 使用`libs.versions.toml`管理依赖版本
- 使用`build-logic`模块管理构建逻辑
- 使用`gradle.properties`管理项目配置
- 使用`proguard-rules.pro`配置混淆规则

### 发布规范
- 使用`release`构建类型发布
- 使用`playdebug`构建类型测试
- 使用`local`构建类型本地开发
- 使用`google-services.json`配置Firebase

## 禁止事项

### 不要使用
- TheRouter路由库（已移除）
- EasyLog日志库（已移除）
- 过时的Android API
- 硬编码的字符串和数字
- 全局变量和单例模式（除非必要）
- 第三方路由库（使用原生Intent导航）

### 不要做
- 在UI线程执行耗时操作
- 在Compose中直接调用网络请求
- 使用`var`声明可变状态（使用`StateFlow`）
- 忽略协程异常处理
- 在Activity中直接处理业务逻辑
- 使用未经审核的AI模型或服务
- 忽略AI生成内容的标识要求
- 违反数据隐私保护法规
- 使用过时的Android API
- 忽略无障碍访问要求

## 最佳实践 (2025.10最新)

### 组件设计
- 保持组件单一职责
- 使用参数化组件
- 避免深层嵌套
- 使用`Modifier`链式调用
- 实施组件复用和模块化设计
- 使用`@Stable`和`@Immutable`注解优化性能

### 状态管理
- 状态不可变
- 使用`copy`方法更新状态
- 避免状态泄漏
- 及时清理资源
- 使用`StateFlow`和`SharedFlow`管理状态
- 实施状态持久化和恢复机制

### 性能优化
- 使用`LazyColumn`处理长列表
- 使用`remember`缓存计算结果
- 避免不必要的重组
- 使用`key`参数优化列表性能
- 实施图片压缩和格式优化
- 使用`ComposeContentType`优化重组
- 监控内存使用和GC压力

### 测试策略
- 单元测试覆盖业务逻辑
- UI测试覆盖用户交互
- 集成测试覆盖端到端流程
- 性能测试覆盖关键路径
- 使用MockK进行依赖模拟
- 使用Koin测试模块进行依赖注入测试
- 实施自动化测试流水线
- 使用Jetpack Compose测试工具

### AI集成最佳实践
- 明确标识AI生成的内容
- 实施AI内容审核机制
- 确保AI决策的可解释性
- 遵循最新的AI伦理标准
- 实施用户同意和透明度机制
- 监控AI模型性能和准确性

### 可持续发展
- 优化应用大小和启动时间
- 实施节能和电池优化
- 使用可持续的云服务
- 遵循绿色软件开发原则
- 实施碳足迹监控
