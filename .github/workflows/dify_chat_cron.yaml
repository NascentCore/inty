# CREATED_BY_AGENT
name: Dify 定时聊天调用

on:
  schedule:
    # 每日 UTC 00:00 执行
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  call_dify:
    # 使用自建 runner，可直接访问数据库
    runs-on: inty-prod-server-gcp

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r scripts/requirements.txt
          python -m pip install -r requirements.txt

      - name: 调用 Dify API
        env:
          DIFY_API_KEY: ${{ secrets.DIFY_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          PYTHONPATH: .
        run: |
          python scripts/run_dify_chat.py --config sync_agents_dev_to_prod/config.yaml.example
