# 消息生图上下文优化

- [ ] 消息生图的上下文精细化改进：原则是一定包括角色头像，其他图片按时间窗口加入，但至少得有一张
      - 角色的形象照片应该作为第一张生图，在用户第一次生图时加入上下文，此时的上下文是：
        头像、形象照片、用户照片、消息（最近几条）、其他提示词
      - 当已经有生图时：
        - 如果上一次生图的记录包含在消息记录里，则自动包含，需要能够将图片数据作为 Image Part 加入到 API 调用参数里
        - 如果上一次生图记录不包含在消息记录里，则需要手动添加到调用参数里
