＃ 核

## 光标摘要

- 目录用途：汇集核心能力与跨模块基础设施。
- 关键子目录/文件:
  -`config.py`: 应用配置集中管理（环境变量、常量）。
  - `logging.py`: 日志配置与封装。
  - `voice/`: 语音合成/播放相关核心模型。
  - `agent/`: 智能体/角色核心提示与封装。
  - `prompting/`: 角色、个性与表达等提示词素材库。
  - 其他: `chat.py` 等面向聊天的核心逻辑。
