"""Companion Harness organizes Inty's companion runtime, memory, tools, and LLM boundaries.

The package is the backend core for long-term companion behavior: it coordinates
turn execution, prompt/system hierarchy, provider access, persisted memory, and
environment stimuli into one anthropomorphic companion harness.
"""
