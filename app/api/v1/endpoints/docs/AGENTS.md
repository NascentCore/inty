# Docs of API endpoints

- Each `*.md` file corresponds to the `*.py` file in the parent dir,
  document the API endpoint in the `py` file
- Always use freshly created DB session when reading database to verify persisted data
- Always assume backend server accessible at `localhost:8000`
