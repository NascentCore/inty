"""
SQLAlchemy table model package for the Inty backend.

Table definitions live in sibling modules and are imported from their defining
modules. Metadata consumers load those modules explicitly so package import stays
free of forward imports and re-exports.
"""
