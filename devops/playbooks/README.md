<!-- CREATED_BY_AGENT -->

# DevOps playbooks

Reusable runbooks for operational changes. Each playbook uses placeholders; fill in values per migration.

- [migrate-config-secret-to-env.md](./migrate-config-secret-to-env.md) — move a secret from baked `config.yaml` to env + GitHub Environment secret (1Password as source of truth).
- Example: IntelliMate backend prod OpenRouter key migration (2026-08).
