# Overrides values defined in app/core/config.py
# All values are intended to be used in testing environment on CI.

app:
  debug: true
  backend_cors_origins: ["http://localhost:3000"]
  environment: "test"

logging:                                                                         
  level: "DEBUG"

database:
  host: "localhost"

google_oauth:
  client_id: "<your-google-client-id>"
  client_secret: "<your-google-client-secret>"
  redirect_uri: "http://localhost:8000/api/v1/auth/google/callback"

agent:
  api_key: "<fill-in-your-openrouter-api-key>"
  langchain_api_key: "<fill-in-your-langchain_api_key>"

gcs:
  use_fake_gcs: true
  bucket: "inty-test"

firebase:
  service_account_path: ".secrets/firebase-service-account.json"

google_play:
  webhook_secret: "<fill-in-your-webhook-secret>"

elevenlabs:
  api_key: "<your-elevenlabs-api-key>"

cloudflare:
  enabled: true
  domain: "cdn.example.com"

gemini_live:
  enabled: false
  # Live Chat 用量限制（测试环境由 TEST_ENVIRONMENT_LIMIT 放宽）
  free_user_agent_limit: 5
  sub_user_agent_limit: 20
  free_user_duration_per_agent_24h: 60
  sub_user_duration_per_agent_24h: 300
