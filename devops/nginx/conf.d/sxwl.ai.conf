# Production instance of inty backend.

# 注意：gzip 配置已在主配置文件 /etc/nginx/nginx.conf 的 http 块中定义
# 如需更详细的 gzip 配置，请修改主配置文件
server {
    server_name app.inty.cc;
    client_max_body_size 100M;
    location / {
        proxy_pass http://localhost:8100;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/app.inty.cc/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/app.inty.cc/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = app.inty.cc) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    server_name app.inty.cc;
    listen 80;
    return 404; # managed by Certbot
}

server {
    server_name dev.inty.sxwl.ai;
    client_max_body_size 100M;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/dev.inty.sxwl.ai/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/dev.inty.sxwl.ai/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = dev.inty.sxwl.ai) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    listen 80;
    server_name dev.inty.sxwl.ai;
    return 404; # managed by Certbot
}

# IntelliMate后端开发环境版本反向代理
server {
    server_name dev.app.inty.cc;
    client_max_body_size 100M;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/dev.app.inty.cc/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/dev.app.inty.cc/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = dev.app.inty.cc) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    listen 80;
    server_name dev.app.inty.cc;
    return 404; # managed by Certbot
}

# iMate dev 后端实例反向代理。GitHub环境`imate-dev`
server {
    server_name dev.imate.sxwl.ai;
    client_max_body_size 100M;
    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/dev.imate.sxwl.ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dev.imate.sxwl.ai/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://localhost:8200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# imate-dev环境后端实例反向代理
# TODO：用于替代dev.imate.sxwl.ai，dev.imate.sxwl.ai应该被删除
server {
    server_name dev.imate.inty.cc;
    client_max_body_size 100M;
    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/dev.imate.sxwl.ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dev.imate.sxwl.ai/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:8200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
server {
    server_name dev.imate.sxwl.ai dev.imate.inty.cc;
    listen 80;
    location /.well-known/acme-challenge/ {
        root /var/www/inty.cc;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# imate-prod环境后端服务实例反向代理
server {
    server_name imate.inty.cc;
    client_max_body_size 100M;

    location / {
        proxy_pass http://127.0.0.1:8301;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/imate.inty.cc/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/imate.inty.cc/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}
server {
    server_name imate.inty.cc;
    listen 80;
    location /.well-known/acme-challenge/ {
        root /var/www/inty.cc;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# dev环境ops平台后端服务实例反向代理
server {
    server_name dev.ops.inty.cc;
    client_max_body_size 100M;
    location / {
        proxy_pass http://localhost:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/dev.ops.inty.cc/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/dev.ops.inty.cc/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    server_name dev.ops.inty.cc;
    listen 80;
    location /.well-known/acme-challenge/ {
        root /var/www/inty.cc;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

# prod环境ops平台后端服务实例反向代理
server {
    server_name ops.inty.cc;
    client_max_body_size 100M;
    location / {
        proxy_pass http://localhost:8101;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/ops.inty.cc/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/ops.inty.cc/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = ops.inty.cc) { return 301 https://$host$request_uri; }
    server_name ops.inty.cc;
    listen 80;
    return 404; # managed by Certbot
}

# IntelliMate网页版App的反向代理
server {
    server_name intellimate.app www.intellimate.app;
    root /var/www/intellimate.app;
    index index.html;

    # API 反向代理到后端服务
    location /api {
        proxy_pass https://app.inty.cc;  # 后端 API 服务 URL
        proxy_http_version 1.1;
        proxy_set_header Host app.inty.cc;  # 改为目标主机名
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # 特别重要的认证相关头信息
        proxy_set_header Authorization $http_authorization;
        proxy_pass_header Authorization;
        proxy_set_header Cookie $http_cookie;

        proxy_buffering off;
        proxy_redirect off;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存策略（带 hash 的文件名可以长期缓存）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # HTML 文件不缓存（确保更新及时生效）
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/intellimate.app/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/intellimate.app/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = www.intellimate.app) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    if ($host = intellimate.app) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    server_name intellimate.app www.intellimate.app;
    listen 80;
    return 404; # managed by Certbot
}

# 公司官网
server {
    server_name inty.cc www.inty.cc;
    root /var/www/inty.cc;
    index index.html;
    location / {
        try_files $uri $uri/ =404;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/inty.cc/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/inty.cc/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = www.inty.cc) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    if ($host = inty.cc) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    server_name inty.cc www.inty.cc;
    listen 80;
    return 404; # managed by Certbot
}

# 公司官网副本，用于与sxwl.ai域名对应
server {
    server_name sxwl.ai www.sxwl.ai;
    root /var/www/inty.cc;
    index index.html;
    location / {
        try_files $uri $uri/ =404;
    }
    listen 443 ssl http2; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/sxwl.ai/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/sxwl.ai/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = www.sxwl.ai) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    if ($host = sxwl.ai) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    server_name sxwl.ai www.sxwl.ai;
    listen 80;
    return 404; # managed by Certbot
}
