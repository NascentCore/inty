# User

**Record user's preferences in this markdown file**

- Companion WebSocket：隐式问候 **仅** **`user_signed_on` + `implicit_greeting` + `message_id`**；**不再接受** 聊天帧 **`IMPLICIT_USER_SIGNED_ON`**（服务端 **400**）。
