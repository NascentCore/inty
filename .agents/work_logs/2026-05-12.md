# 2026-05-12

## LangSmith run 019e1a9f-0a80-7ab1-95ef-69fd8fbc3e45

- 使用 `.cursor/skills/langsmith-download-run/scripts/download_run.py` 下载 trace；`config.yaml` 默认 project 与 run 元数据不一致时需 `--project-name inty-backend-local-yzhao` 才能拉全 5 条 span。
- 产出：`tmp/langsmith_traces/019e1a9f-0a80-7ab1-95ef-69fd8fbc3e45_full.json`（完整 trace）、`tmp/langsmith_runs/019e1a9f-0a80-7ab1-95ef-69fd8fbc3e45_tree.json`（`read_run` + `load_child_runs`）。

## inner_tick_mechanism（maintenance_tool_solo）

- 在 `app.utils.config` 增加 `InnerTickMechanism` 与 `FeaturesConfig.inner_tick_mechanism`（YAML `app.features.inner_tick_mechanism`）；维护性 inner tick 可选跳过前台 envelope LLM，`start_tool_background_job` 不变；`CompanionConfig` / `companion_chat_service` LRU fingerprint 透传。
- 测试：`tests/app/utils/test_config.py`、`tests/app/core/agentic_kernel/companion/test_turn_async_dual_llm.py`。

## 配置迁移任务工单

- 新增 `.agents/maintenance/P2_CONFIG_DATACLASS_PYDANTIC_MIGRATION.md`，按配置类拆分 `app/utils/config.py` 的 dataclass→Pydantic 增量迁移任务与依赖顺序，供后续自动化执行。

Follow-ups: 无

## test_agents flaky 失败收敛

定位 `test_agents.py` 的 `StaleDataError` 来自测试夹具持有带乐观锁版本的 `Agent` ORM 实例，同时 `create_agent` 触发后台 opening TTS 写同一行；将目标测试改为显式空 opening，并用 SQL `UPDATE` 写夹具字段。

- `TestClient.create_agent` 新增 `opening` 参数，保持原默认值；目标测试传 `opening=""` 避免后台 TTS 干扰
- 推荐测试的 `points`、`exclusive_photos`、`background` setup 改为直接 `UPDATE`，避免 ORM 版本 stale
- `energy_points` 推荐测试创建前将当前用户设为 superuser，使其真正覆盖 `/recommend` 的 superuser-public 过滤；首屏改为 `page_size=100` 并断言 5 个 fixture agent 全部返回
- `/review` 后修正 `test_recommend_agents_never_returns_private_even_for_superuser` 的 superuser cleanup，恢复测前原值而非固定写 `False`
- 已跑 `pytest tests/app/api/v1/endpoints/test_agents.py -q`：当前无 live backend，2 passed / 12 skipped

Follow-ups: 启动本地后端后复跑 `pytest tests/app/api/v1/endpoints/test_agents.py`

## LangSmith：download_run 迁至 scripts/

将 `download_run.py` 从 `.cursor/skills/langsmith-download-run/scripts/` 迁至仓库根 `scripts/`，并同步文档引用。

- `git mv` 至 `scripts/download_run.py`，移除技能下空 `scripts/` 目录。
- 更新 `langsmith-download-run`、`investigate-imate-ws-backend` 的 SKILL.md 与 `langsmith_find_companion_run_by_user_msg_uuid.py` 文档字符串中的路径。
- 已验证 `python scripts/download_run.py --help` 正常。

Follow-ups: 无

## companion WS：inner tick 断线后停止（回归测试）

- `chat.py`：`max(5.0, …)` 改为模块级 `_COMPANION_WS_INNER_TICK_POLL_FLOOR_SECONDS`（默认 5.0），供测试 monkeypatch。
- `test_chat.py`：`test_chat_websocket_companion_inner_tick_worker_stops_after_disconnect`（spy `_try_fire_*`、压低 poll floor + `companion_ws_proactive_heartbeat_poll_seconds`、断线后计数不变）。
- 已跑：`pytest tests/app/api/v1/endpoints/test_chat.py -k inner_tick`。

Follow-ups: 无

## conversation-grid-matplotlib：draw_labeled_grid 迁至 scripts/

将 YAML 网格图脚本从技能子目录移到仓库根 `scripts/`，并同步 SKILL 与 agentic_kernel 文档中的调用路径。

- `git mv` 至 `scripts/draw_labeled_grid.py`，移除技能下空 `scripts/` 目录
- 更新 `.cursor/skills/conversation-grid-matplotlib/SKILL.md` 与 `docs/agentic_kernel/conversation_intelligence_realtime_grid.md` 中的 `python3` 路径
- 在临时 venv 中安装技能 `requirements.txt` 并跑生成 PNG 冒烟通过

Follow-ups: 无

## Fixed the highest-priority open Python style maintenance TODO for companion significance envelope parsing.

- Chose `.agents/maintenance/P2_FIX_PYTHON_STYLE_VIOLATIONS.md` and completed the first open 2026-05-11 TODO.
- Narrowed Pydantic envelope validation failures to `ValidationError`.
- Logged provider `model_dump()` extraction failures instead of silently returning no candidates.
- Marked the maintenance TODO complete.
- Follow-ups: none.

## feat(chat-ws): REPL 重连上报 `ws_conn_dropped` 与后续 `user_signed_on`

- `app/schemas/chat_websocket.py`：`ChatWsWsConnDroppedFrame` / `ChatWsWsConnDroppedAckFrame`；`app/api/v1/endpoints/chat.py` 消费控制帧并向 companion `CHAT_LOGS.md` 追加一行；`/ws/verify` 返回 `not_supported`。
- `tools/inty_v2_repl/backend_chat_ws.py`：传输断开后下次成功连接先发 `ws_conn_dropped` 再等 ack（`INTY_V2_BACKEND_WS_CONN_DROPPED_ACK_TIMEOUT_SEC`），再发 `user_signed_on`（仅首次 `implicit_greeting: true`）。
- 文档：`app/schemas/AGENTS.md`、`tools/inty_v2_repl/AGENTS.md`。
- 测试：`pytest tests/app/api/v1/endpoints/test_chat.py::test_chat_websocket_ws_conn_dropped_appends_chat_logs_line`、`::test_chat_websocket_verify_ws_conn_dropped_not_supported` 通过。

Follow-ups: 无

## inty_v2_repl：`ws_conn_dropped` ack 超时与服务器配置解耦

REPL 传输层不再 import `app.core.config`；`ws_conn_dropped` ack 等待固定为包内常量 5s；删除 `IntyV2ReplConfig` 与 `Config.inty_v2_repl`；更新 `AGENTS.md`、`devops/config.yaml.test`、本地 REPL skill；已跑 `pytest tests/tools/inty_v2_repl/test_backend_chat_ws.py`、`tests/app/utils/test_config.py`。

Follow-ups: 无

## chat WebSocket：客户端断线容忍（不拖垮 uvicorn）

- `chat.py`：`_WS_RECEIVE_TEXT_NOT_CONNECTED_MSG` / `_is_ws_receive_text_not_connected_runtime_error`；`recv_task.result()` 与 `/ws/verify` 收包捕获 `WebSocketDisconnect` 与 Starlette 固定 `RuntimeError`；`tool_bg` 分支对同文案 `RuntimeError` 仅 debug；`_shutdown_chat_ws_outbound_pump` 对 `WebSocketDisconnect` 单独 debug。
- `chat_websocket_session.py`：`send_json` 遇 `WebSocketDisconnect` 则 debug 并退出 pump 循环。
- 测试：`test_chat.py`（helper + verify + `/ws` ping 后断线）、`test_chat_websocket_session.py::test_chat_ws_outbound_pump_send_json_disconnect_exits_cleanly`；已跑 `pytest tests/app/api/v1/endpoints/test_chat.py -k "websocket or ws_conn or inner_tick or not_connected or verify_receive"` 与 `test_chat_websocket_session.py`。

Follow-ups: 无
